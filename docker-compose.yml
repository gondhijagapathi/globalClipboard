services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./globalClipboard.db:/app/globalClipboard.db
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
