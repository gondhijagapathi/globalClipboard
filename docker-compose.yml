services:
  app:
    build: .
    ports:
      - "7777:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./globalClipboard.db:/app/globalClipboard.db
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
